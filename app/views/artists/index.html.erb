<div class="row text-center">
  <% @artists.each do |a| %>
  <div class="col-sm-4 col-md-4 col-lg-3">
    <div class="thumbnail">
      <img src="<%= a.image_url %>" alt="...">
      <div class="caption">
        <h3><%= a.name %></h3>
        <p><a href="#" class="btn btn-primary btn-md btn-block" role="button">Ver más</a></p>
      </div>
    </div>
  </div>
  <% end %>
</div>
